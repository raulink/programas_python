Para poder generar los pdf se debe instalar primero weasyprint

```
pip install weasyprint
```

Se define un template html y css, en el html se denotan las variables
entre corchetes, ejemplo:

```
<p>{variable}</p>
```